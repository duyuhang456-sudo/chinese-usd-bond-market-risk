# 实习项目周计划（第 4 周）

**时间：2026-09-28（周一）~ 2026-10-02（周五）**
**阶段：第四阶段——工具整合与终期成果输出**
**所属项目：中资投资级美元债市场风险计量与压力测试预研工具**

> 本计划由**《中资投资级美元债市场风险计量工具 需求文档》第四章「第四阶段」**逐条拆解而来，
> 输入为前三阶段全部交付物。原阶段目标原文：**「整合所有功能模块，输出完整的预研原型工具与体系化研究报告。」**
>
> **需求文档第四阶段的三项交付物**（原文，本计划以此为验收依据）：
> 1. 轻量化风险计量原型工具（**Python 代码包，含可视化模块与使用说明**）
> 2. 《中资投资级美元债风险计量与压力测试预研报告》终稿
> 3. 结项汇报 PPT（**支撑 10–15 分钟的成果展示**）
>
> 质量要求原文：「工具结构清晰，可直接运行，核心结果可视化呈现；报告逻辑完整，专业规范，
> 结论明确；汇报材料重点突出，方法与结论清晰。」
>
> **本阶段前置决策（本周开始前已定）**：
> 1. **原型工具形态 = 一键 CLI + 阶段选择**。不做交互式界面（Streamlit 等会引入新依赖，
>    与需求「轻量化工具」的表述偏离），不做自包含 HTML 报告（列为周六备选）。
> 2. **结项汇报材料形态 = 逐页大纲 + 讲稿**，不直接产出 `.pptx`。
> 3. **判定阈值以主判定线 `es_1d_99_pct` 定稿**（阶段二已登记的滚动 250 日基准：主口径 0.6659%、
>    次口径 0.7024%）。同源交叉核对列 `es_hd_99_pct`（全样本无条件经验值：主 0.8573%、次 1.0646%）
>    与主判定线的差异 **写进局限、不静默省略**。该取舍仍待老师确认，返工面已隔离（见 §四 风险 1）。
> 4. **阶段三挂账的一条技术项本周必须清掉**：历史与补充情景的 `credit_range_lo_pct` /
>    `credit_range_hi_pct` 现为窗口级量，与 1 日阈值不同口径（`docs/week3_report.md` §6.5 第 1 条）。
>    终期报告要用这两列，不能带着未对齐的口径进去。
>
> 输入主线：`results/` 与 `factors/` 下的全部 CSV（阶段二 17 张 + 阶段三 9 张）→
> 一键运行入口 → 使用说明 → 终期预研报告 → 结项汇报逐页大纲。
> **本阶段不产生新的计量结论**，只做整合、成文与呈现；凡终期报告里出现的新数字，必须回到 CSV 复算。

---

## 一、本周定位

| 周次 | 阶段 | 目标 | 状态 |
|---|---|---|---|
| 第 1 周（9/8–9/11） | 一：数据体系搭建与风险因子拆解 | 6 类数据清洗对齐 → 三因子库 → 描述统计收口 | 已完成 |
| 第 2 周（9/14–9/18） | 二：风险计量模型开发与回测验证 | 参数法 & 历史模拟法 VaR + 滚动回测 + 统计检验 + 基准口径定案 | 已完成 |
| 第 3 周（9/21–9/25） | 三：压力测试框架构建与测算分析 | 情景库 + 三路径映射 + 损失 / 回撤 / 贡献度 + 五项稳健性 | 已完成（含 1 日持有期重做与 28 处缺陷纠正） |
| **第 4 周（9/28–10/2）** | **四：工具整合与终期成果输出** | **一键运行工具 + 终期预研报告终稿 + 结项汇报材料** | **本周** |

### 交付物落点对照

| 需求文档第四阶段交付物 | 本周落点 | 本周开始前的现状 |
|---|---|---|
| 轻量化原型工具（含可视化模块与使用说明） | `code/run_all.py` + `docs/tool_usage.md` | 24 个脚本 / 6179 行；**无入口脚本、全仓库 0 处 `argparse`**；`RES` / `FACT` 路径常量在 **13 个脚本**里各自重复定义；README 里靠手敲 12 条命令复现 |
| 《…预研报告》终稿 | `docs/final_report.md` | 未开始；前三阶段文档齐备，可作为提炼来源 |
| 结项汇报 PPT | `docs/final_ppt_outline.md`（逐页大纲 + 讲稿） | 未开始 |

### 需求文档「功能范围」六模块的收口状态

需求文档 §2.2 要求覆盖 6 个核心功能模块，本阶段是收口：

| # | 模块 | 状态 | 本周动作 |
|---|---|---|---|
| 1 | 数据采集与标准化处理 | 已完成 | 纳入一键运行链路的 phase1 段 |
| 2 | 风险因子拆解 | 已完成 | 同上 |
| 3 | 风险计量（参数法 / 历史模拟法，多置信水平） | 已完成 | 纳入 phase2 段 |
| 4 | 模型验证（滚动窗口回测 + 统计检验） | 已完成 | 纳入 phase2 段 |
| 5 | 压力测试（历史 / 假设，多梯度） | 已完成 | 纳入 phase3 段 |
| 6 | **结果输出（核心指标可视化 + 标准化研究报告生成）** | **未收口** | **本周主体工作**——一键入口、使用说明、终期报告、汇报材料 |

---

## 二、每日计划

### 9/28（周一）工具整合 I：路径收敛 + 一键入口

**当日目标**：把 24 个脚本从「README 里手敲 12 条命令」变成「一条命令跑完全链路」。

**具体工作**：

1. **依赖盘点**，产出依赖图。已核实的现状：23 个脚本 import `common`；
   `stress_robustness.py` 还 import `stress_impact`（唯一的真模块依赖）；
   `common.py` 只导出了 `RAW` 一个路径常量，而 **`RES` / `FACT` 在 13 个脚本顶部各自重复定义**。
   `argparse` 全仓库 **0 处**——没有任何脚本支持命令行参数。
2. **把 `RES` / `FACT` 收敛到 `common.py`**（与既有的 `RAW` 并列），13 处改为 import。
   这是后续加 `--out-dir` / `--check` 类参数的前提：路径不收敛，入口脚本就无法统一改写输出位置。
3. **写 `code/run_all.py`**：
   - 依赖顺序：`download_*` → `clean_data` → `outlier_detect` / `adjudicate_outliers` →
     `build_factors*` / `staleness_remedy` → `prep_phase2` → `var_parametric` → `var_historical` →
     `var_backtest` → `recommend_baseline` → `verify_delta_transmission` → `stress_scenarios` →
     `stress_impact` → `stress_robustness`
   - 参数：`--only phase1|phase2|phase3`（可组合）、`--skip-download`（**默认开启**，取数需联网）、
     `--check`（只跑断言不写盘）
   - **区分取数失败与计算失败**：取数脚本失败记为警告并继续；计算脚本失败必须终止，
     不得带着缺料往下跑。
4. **新增入口级对账断言**：全链路跑完后，校验阶段二 17 张 + 阶段三 9 张 CSV 的行列维度
   与 README 声明逐个一致。这是防「脚本改了、README 没跟」的第一道闸。

**当日产出**：`code/run_all.py`；收敛后的 `common.py`；依赖图（并入次日使用说明）

**验收**：清空 `results/` 后，`python code/run_all.py --only phase2,phase3` 一条命令重建全部产物；
连跑两次产物逐字节一致。

---

### 9/29（周二）工具整合 II：使用说明 + 清挂账技术项

**当日目标**：需求要求工具「含可视化模块与使用说明」；同时清掉阶段三挂账的技术项。

**具体工作**：

1. **可视化模块的处理方式——不抽独立模块**。现有绘图逻辑内嵌在 `var_*.py` / `stress_*.py` 里，
   且已在现链路上跑通；抽成 `plots.py` 会引入回归风险，而本阶段的时间应花在成文上。
   改为在使用说明里建一张**三列对应表**：哪张图 ← 由哪个脚本产出 ← 消费哪张 CSV。
   这样「可视化模块」在文档层面是可定位、可单独复现的。
2. **清掉 `docs/week3_report.md` §6.5 第 1 条**：把历史与补充情景的 `credit_range_lo_pct` /
   `credit_range_hi_pct` 由窗口级量重算为 1 日量。
   **动之前先全仓库 grep 这两列的引用点**——改写 `stress_impact.csv` 会连带影响阶段三三份
   文档里引用它们的地方（`phase3_stress_testing.md`、`week3_report.md`、`week3_report_formal.md`）。
   改数据必须同步改文档，这正是本次 28 处缺陷里「重做后未同步」那一类的成因。
3. **写《工具使用说明》**（`docs/tool_usage.md`）：环境搭建（Python 版本、依赖清单与安装命令）、
   一键运行、分阶段运行、脚本职责表、输出物清单（CSV ↔ 图 ↔ 文档的对应）、
   硬断言的语义与报错含义、已知边界与不可外推的范围。

**当日产出**：`docs/tool_usage.md`；信用区间两列的 1 日重算结果与引用点同步修正

**验收**：按使用说明在一个干净 venv 里走通全链路；信用区间两列与 1 日阈值的口径一致性有断言保证。

---

### 9/30（周三）终期预研报告 I：前三部分

**报告落点**：`docs/final_report.md`，题《中资投资级美元债风险计量与压力测试预研报告》
**结构**：严格按需求文档第五章给的五部分，本日完成前三部分。

1. **项目背景与研究意义**——从需求文档 §1.2 项目背景与 §1.3 项目定位出发，
   写清「为什么是新兴市场、为什么是预研性质」。
2. **数据体系与风险因子框架说明**——把阶段一的六类数据、清洗规则（日期对齐 / 缺失值 /
   异常值）、三因子构建（利率剥离 / 信用剥离 / 汇率）串成一条链，
   来源为 `docs/phase1_data_description.md`、`docs/data_cleaning.md`、`docs/factors.md`。
3. **风险计量模型实现与验证结果**——两个模型族（参数法 / 历史模拟法）、滚动窗口回测、
   失败率 + Kupiec + Christoffersen 检验、**基准口径定案**（HS250 统管双口径双置信度），
   来源为 `docs/phase2_spec.md`、`docs/phase2_model_validation.md`。

**必须单独设一节「需求文档与实现的偏差及原因」**——这是验收标准「数据来源合规可追溯」
的正面回应。至少含四条，全部有据可查：

| # | 需求文档原文 | 实际实现 | 原因 |
|---|---|---|---|
| 1 | §3.2 标的行情：iShares 中国投资级美元债 ETF，代码 `MCHB` | **9141.HK（USD 柜台）** | 经 Yahoo 官方接口核实，`MCHB` 实为 Mechanics Bancorp（美国银行股，EQUITY/Nasdaq）；该数据已弃用并单独留档标注 |
| 2 | §3.4 信用利差基准：`BAMLEMIBHGLCPIEYPI` | `BAMLEMIBHGCRPIOAS` | 前者为扫描件 OCR；以实取代码为准。该系列自 2023-09 才有数据，历史偏短属源数据限制 |
| 3 | 第三阶段历史情景举例含「2020 年疫情冲击」 | 以样本内同级冲击（2025-04 关税）替代 | 2020 年早于因子表起点（2021-08-03），无法提取该期间因子波动；不伪造数据、不用近似事件冒充 |
| 4 | 全文未出现「持有期」一词 | 主情景库取 1 日持有期 | **老师口头指示**（「主情景库保持 1 日持有期，与 VaR 口径对齐」），非需求文档要求；属自定口径，已在文档中如实标注来源 |

**当日产出**：`docs/final_report.md` 第 1–3 部分

---

### 10/1（周四）终期预研报告 II：后两部分 + 全文收口

4. **压力测试框架设计与核心发现**——本部分要写进 1 日持有期重做之后的口径：
   情景库与 VaR 同期限因而可直接比较；**三条被翻转的结论及各自机制**
   （缓冲敏感性 = 缓冲占阈值比例由 40.2% 升至 50.5%；信用区间 = 尺子变了而非证据变强；
   次口径尾部 = 统计性质随期限变化）；28 处缺陷的发现与处理；
   尾部判定改用 **× 99% 期望损失倍数** 这一连续统计量。
5. **研究结论与方法优化建议**——结论分三层陈述：

| 层级 | 含义 | 本周已识别的内容 |
|---|---|---|
| **稳健的** | 不随口径 / 窗口 / 缓冲档位变化 | 情景库 1 日覆盖度（主口径库内最深 2.5363% 对实测最差单日 1.3449%） |
| **参考的** | 有方向性但不可当点估计 | 信用维度整体（M3 估计样本起于 2023-09）、假设情景绝对水平（锚点窗长极差汇率达 3.9580pp） |
| **不可外推的** | 明确指出边界 | δ 线性传导对信用情景不可外推；2022 年残差为代理量（`doas_bp` 2022 年整年缺失） |

**口径定案**：判定阈值以主判定线 `es_1d_99_pct` 为准；
同源交叉核对列 `es_hd_99_pct` 与主判定线的差异（主口径 +28.7%、次口径 +51.6%，两者样本不同：
一个是滚动 250 日样本、一个是压力参照样本）**写进局限**，不省略、不替它编解释。

**当日产出**：`docs/final_report.md` 全文

---

### 10/2（周五）结项汇报材料 + 全仓库终检

**当日目标**：需求要求「梳理核心研究发现，支撑 10–15 分钟的成果展示」。

**具体工作**：

1. **写 `docs/final_ppt_outline.md`**：逐页给出 标题 / 放哪张图哪张表 / 口播要点 / 时长，
   并给每页标注**可删优先级**（保证能压到 10 分钟）。建议时长分配：

   | 段落 | 时长 |
   |---|---|
   | 背景与问题 | 2 min |
   | 数据体系与因子框架 | 2 min |
   | VaR 与回测验证 | 3.5 min |
   | 压力测试与核心发现 | 4.5 min |
   | 结论、局限与方法建议 | 3 min |
   | **合计** | **15 min** |

2. **讲稿重点放在「三条被翻转的结论」**上。这是全项目最有方法论价值的一段：
   它演示了「先验证、再下结论」，以及一个口径参数如何沿传导链改变最终结论。
   比罗列结果更能体现研究深度（对应验收标准第 3 条）。
3. **全仓库终检**：grep 旧值（`54bp` / `2.7459` / `1.4354` / `0.6548` / `2.6784` / `持有期待确认`）；
   抽查数字溯源；确认 README 已登记全部新文档；确认 `.gitignore` 仍排除两份内部复盘版。

**当日产出**：`docs/final_ppt_outline.md`；终检记录

---

## 三、备选缓冲

- **10/3（周六机动）**：进度滞后时补报告或汇报页面；超前则做**自包含 HTML 报告**——
  把全部图与关键表合成单页单文件（内嵌图片、不联网），便于交付与答辩现场演示。
  本次未选入主线，是因为它属于呈现层的锦上添花，而使用说明与终期报告是需求文档的硬交付物。

---

## 四、风险与应对

1. **口径遗留问题仍未获老师确认** → 已定按主判定线 `es_1d_99_pct` 定稿。
   为控制返工面，该阈值的出现位置被限制在三处：终期报告第 4 部分、
   `code/stress_impact.py` 的常量、工具输出表的列名。
   若老师另有口径，只需改这三处，不必全文返工。
2. **抽可视化模块的回归风险** → 原则是**只搬不改**。本周选择**不抽**独立模块，
   改为在使用说明里建「图 ↔ 脚本 ↔ CSV」对应表。若后续一定要抽，搬完必须逐图比对再替换。
3. **信用区间 1 日重算会改动已发布文档引用的数字** → 改 `stress_impact.csv` 两列前
   先 grep 全部引用点，改完同步修正文档。**不得只改数据不改文档**——
   这正是本次 28 处缺陷里「重做后未同步」那一类的成因，不能再犯。
4. **终期报告引入新数字** → 终期报告是**体系化叙述**，不是新一轮计算。
   不得出现前三阶段文档中不存在的新结果；凡新数字必回 CSV 复算，复算不出的写
   「目前资料不足，无法确认」，不留一个读起来顺但指不到来源的数。
5. **一键运行的幂等性** → 取数脚本依赖网络。`run_all.py` 须能区分取数失败与计算失败，
   默认 `--skip-download`；计算类脚本失败必须终止而非继续。
   验收要求「连跑两次产物逐字节一致」，这是「可直接运行」的量化标准。
6. **汇报时长超限** → 物料按 15 分钟排，但每页标注可删优先级，
   保证压缩到 10 分钟时不丢主线（三条翻转结论 + 三层结论分级）。
7. **终期报告与前序文档的口径再度失同步** → 报告里的每个损失读数必须带口径标签
   （窗内最差单日 / 窗内累计 / 窗内最大回撤）。不带标签的读数视为未完成引用——
   本次 28 处缺陷里有 5 处正是缺标签造成的。
