# 实习项目周计划（第 2 周）

**时间：2026-09-14（周一）~ 2026-09-18（周五）**
**阶段：第二阶段——风险计量模型开发与回测验证**
**所属项目：中资投资级美元债市场风险计量与压力测试预研工具**

> 本计划由需求文档第四章「第二阶段」拆解而来，输入全部来自第 1 周（阶段一）交付。
> 前置口径已定案（详见 docs/staleness_remedy.md、docs/phase1_data_description.md）。
>
> **计划修订（依据导师对 9/14 进度的反馈追加）**：
> 1. **VaR 输入先做总收益（复权）修正**——除权 NAV 的日收益把「分红除息跳空」计成了市场波动，会系统性高估 σ，进而抬高 VaR 基准；
>    正式输入升级为**加回分红后的复权日收益**（USD 主口径），并据分红记录复核（见 9/14 增补②）。
> 2. **汇率改「双口径」设计，不再置零了事**——主口径 = **USD 本位**（汇率暴露 = 0，对标持仓自身的市场风险，VaR 上报口径）；
>    次口径 = **人民币视角总收益**（= USD 复权收益 × CNY/USD，汇率暴露 = 1），让「利率 + 信用 + 汇率」三类风险进入同一总收益框架、
>    可做全维度压力测试（衔接第 3 周）。
> 3. **回测检验升级**——**Christoffersen 独立性检验为必做项**，与 Kupiec LR_uc 并列输出，验证两点：整体覆盖率是否准确、违规是否聚集
>    （违规聚集比单纯失败率偏高更严重）；平稳 / 高波动分段同样给这两项。
>
> 输入主线（修订后）：`factor_table_nav.csv`（除权 NAV 因子表，保留）→ **复权扩展** `factor_table_nav_tr.csv`（新增：USD 复权收益、
> 人民币总收益，VaR 建模正式输入）。市价 / 3141 仅作对照，不作 VaR 输入。

---

## 一、本周定位

| 周次 | 阶段 | 目标 |
|---|---|---|
| 第 1 周（已完成） | 一：数据体系搭建与风险因子拆解 | 6 类数据清洗对齐 → 三因子库 → 描述统计收口 |
| **本周** | **二：风险计量模型开发与回测验证** | 参数法 & 历史模拟法 VaR + 滚动回测 + 统计检验 + 模型对比 |
| 第 3 周 | 三：压力测试框架 | 历史/假设情景 + 损失测算与敏感度分析 |
| 第 4 周 | 四：工具整合与终期报告 | 一键运行原型 + 预研报告 + 汇报 PPT |

**阶段目标（需求文档原意）**：实现两种主流 VaR 计量模型，搭建回测验证框架，完成模型效果对比与适用性分析。

**输入资源（第 1 周交付 + 本周修正）**：
- 因子表：`factors/factor_table_nav.csv`（除权 NAV 口径，作为复权修正的底表）→ **`factor_table_nav_tr.csv`（复权扩展：USD 总收益 + 人民币总收益，VaR 正式输入）**；
  利率因子 Δ5Y/Δ10Y、汇率 CNY/USD、信用利差代理（残差）同表。
- 事件时间线 `events/risk_events_timeline.csv`（平稳/高波动分段与异常复核用）；异常判定表 `clean_data/outlier_judgment.csv`
  （16 条「存疑」稳健性复核，默认规则见 9/14 增补④）。
- 分红记录（新增，来源=基金公司官方派息公告，见 9/14 增补②）：9141.HK 各期除息日 + 每单位派息额，用于复权。

**本周末应交付**：
1. 可运行的模型代码包（Python）：参数法 VaR（方差-协方差 + GARCH(1,1) 修正）与历史模拟法 VaR + 滚动回测验证模块，关键逻辑附注释
2. 《模型验证分析报告》：回测结果数据、统计检验结果（失败率 + Kupiec + **Christoffersen**）、模型对比分析与适用性结论

质量线：**模型逻辑正确、代码可一键运行输出结果；回测方法符合行业通用规范（滚动 250 窗 / 步长 1 日 / 失败率 + Kupiec + Christoffersen）；结论有数据支撑、客观反映模型优缺点**。

---

## 二、每日计划

### 9/14（周一）建模口径定稿 + 输入数据刷新

**当日目标：** 把 VaR 计量口径与回测协议写成文档钉死；输入表刷新到最新交易日；**完成分红复权与人民币双口径（导师反馈增补）**；建好模型输出目录与基线。

- **上午**
  1. **数据刷新**：补 CNY/USD 尾 5 交易日（重跑 `code/download_fred.py` → `code/clean_data.py`）。
     **若 FRED 仍未发布**（2026-09-07 复核停在 8/28）：**不伪造、在相应列留空并记档**——USD 主口径 δ_fx=0，缺汇率对主口径 VaR 零影响；
     人民币次口径该段先记 NaN，FRED 补发后自动真值补齐（此项已在 spec §2 记档，需 flat 补齐时一条命令可加）。
  2. **基准组合与因子暴露定稿（双口径）**：
     - 主口径（USD 本位）：组合收益 = 9141.HK 官方 NAV **复权**日对数收益（`etf_ret_tr_pct`），持有期 **1 日**。
       因子暴露 δ：Δ5Y / Δ10Y = 复权收益上的同日回归 β5/β10（等效久期 ≈3.70y，R²=0.757）；信用利差代理暴露 = 1；**汇率暴露 = 0**（美元计价，天然不担 USD 汇率风险）。
     - 次口径（人民币视角）：`etf_ret_rmb_pct = etf_ret_tr_pct + fx_ret_pct`（近似，精确为 (1+r)·(1+fx)−1）。
       暴露 = 主口径 δ 之上**叠加汇率暴露 = 1**；即人民币投资者同时承担资产 USD 市场风险与 CNY/USD 折算风险——三类风险进入同一收益框架，供第 3 周全维度压力测试。
  3. 建模型目录与基线：新建 `results/`（VaR 序列与回测表）；先算**无条件正态 VaR 基线**（1 日 95%/99%），
     主口径（复权 σ_ann 实测 **4.22%**，低于除权口径 4.52%）与人民币次口径（σ_ann 5.11%）各给一组，作为后续两种模型的经验失败率参照。
- **下午**
  4. 撰写 **`docs/phase2_spec.md`（VaR 计量口径与回测协议）**：置信度 95%/99%、持有期 1 日；回测窗 **250 交易日 / 滚动步长 1 日 / 样本外**；
     检验 = **失败率 + Kupiec LR_uc + Christoffersen 独立性（均为必做，后两项 χ²(1)）**；平稳/高波动分段口径（见下）；双口径说明；每种模型的固定随机数与舍入规则（保证可复现）。
  5. **分段定义**：按事件时间线把样本外区间标「高波动段」——2022 加息与缩表、2023-03 SVB/瑞信、2025-04 关税等；其余为「平稳段」。另存一段：阶段一 16 条「存疑」异常日清单，供 9/17 稳健性复查。

- **增补（导师反馈后追加，建模前置）**
  ① **分红除权复核与复权处理**：核验 9141.HK 派息记录（官方公告）；把每期除息日的既定跳空加回，得 USD 总收益（复权）序列；
     对比复权前后 σ / 基线 VaR，量化「除息跳空造成的波动高估」并写进 spec（若核验确认基金为累计份额/不派息，则在 spec 记档并修正阶段一「分红致负漂移」的表述）。
  ② **人民币视角总收益口径**：同表新增 `etf_ret_rmb_pct`（USD 复权收益 × CNY/USD）；CNY 尾 5 日未发布处记 NaN 并记档。
  ③ **利差代理局限说明**：在 `docs/factors.md` 与 spec 补写代理残差的局限（信噪比、与外部 EM OAS 相关仅 ~0.18 且为 EM 级非紧基准、
     只能作为「扣除利率后残差」而非独立可交易利差），明确对 VaR 归因的解释边界。
  ④ **16 条存疑日默认处置规则定稿**：延续「只标注不改数」；默认规则 = **建模必做「含/不含」两版对照**，任何违规日若落入存疑清单，
     先核 3141 同向性与事件时间线再归因，结论不以个别存疑日为准（写入 spec §8 与异常判定文档）。

- **当日验收：** 因子表覆盖至最新交易日（含 CNY 尾 5 日处理记录）；**复权序列 + 人民币次口径序列落地（`factor_table_nav_tr.csv`），
  复权前后 σ 对比打印**；`docs/phase2_spec.md` 落盘（含双口径、Christoffersen 必做、存疑日默认规则、利差代理局限）；
  `results/` 就绪，双口径无条件正态 VaR 基线打印（主口径 99% VaR 0.619% 与除权口径 0.663% 对照完成）。

---

### 9/15（周二）参数法 VaR：方差-协方差 + GARCH(1,1)

**当日目标：** 参数法全谱实现——无条件正态（baseline）→ GARCH(1,1) 条件波动率 → 因子协方差 δ-normal，输出逐日样本外 VaR 预测（双口径）。

- **上午**
  1. **无条件参数法（baseline）**：VaR(1d, c) = −(μ + z_c·σ)，σ = 全样本或扩展窗样本σ（复权主口径 σ，见 9/14 基线）。
  2. **条件波动率 GARCH(1,1)**（`arch` 库 7.2.0）：对主口径复权收益拟合 GARCH(1,1)（先正态，后 Student-t 对照）→ 逐日条件 σ_t → 条件参数法 VaR 95%/99%。
     重点核对：σ_t 能否捕捉 2022/2025-04 等高波动聚集、收敛与平稳性（α+β<1）。
- **下午**
  3. **因子协方差 δ-normal（主口径）**：以 Δ5Y、Δ10Y、利差代理为因子，估计因子协方差 Σ（与回测一致的扩展窗），组合方差 σ_p² = δ′Σδ（δ 来自 9/14）；
     与「组合收益直接法」对照，检验方差是否被三因子解释（应与阶段一利率剥离 R²≈0.67 呼应：约 2/3 方差来自利率因子）。
  4. **人民币次口径参数法（对照）**：用 `etf_ret_rmb_pct` 重复 δ-normal 与 GARCH，暴露 δ_rmb = δ + FX(1)；输出与主口径的对比表
     （人民币口径 σ 应更高，量级由汇率波动 4% 年化贡献）。
  5. 图：`figures/var_garch_sigma.png`（GARCH σ_t vs 无条件 σ，双口径分面）、`figures/var_series_compare.png`（收益 + 两置信度 VaR 线）。

- **当日验收：** `code/var_parametric.py` 可运行，输出 `results/var_parametric.csv`
  （date / 无条件VaR95·99 / GARCH σ_t / 条件VaR95·99 / δ-normal VaR；主口径 + 人民币次口径分列）；
  打印 GARCH 系数与对数似然；图落盘。

---

### 9/16（周三）历史模拟法 VaR

**当日目标：** 经典历史模拟法 250 交易日滚动分位数，配套窗宽与滤波对照，输出逐日样本外 VaR 预测（复权主口径为基准，人民币次口径并列）。

- **上午**
  1. **经典 HS**：对过去 **250 个交易日**复权收益取经验分位（5%、1%）→ 1 日 95%/99% VaR；滚动步长 1，逐日向前预测（样本外）。
  2. **Filtered-HS（对照）**：历史收益先除以当日估计 σ（用 GARCH σ_t）再乘当前 σ_t——检验能否改善高波动期 HS 响应滞后。
- **下午**
  3. **窗宽稳健性**：250 vs 500 vs 750（尾部是否收敛）——呼应阶段一教训「周度仅 265 观测、99% 分位不稳」，NAV 日度 ~1273 观测应明显更稳；
     输出各窗失败率对照，看窗口选择敏感性。
  4. 图：HS VaR 序列 vs 参数法叠加（供 9/17 对比）；经验分布 1%/5% 分位随时间变化（人民币次口径可叠加对照）。

- **当日验收：** `code/var_historical.py` 可运行，输出 `results/var_historical.csv`
  （date / HS-VaR95·99 / Filtered-HS-VaR95·99；主口径 + 人民币次口径分列）；窗宽稳健性表打印；图落盘。

---

### 9/17（周四）滚动回测 + 统计检验 + 模型对比

**当日目标：** 统一滚动回测框架跑通两模型 × 两置信度 × 双口径，失败率 / Kupiec / **Christoffersen** 三件套出数，
分平稳-高波动对比，附阶段一「存疑」日稳健性复核（含/不含默认必做）。

- **上午**
  1. **滚动回测主程序** `code/var_backtest.py`：对 9/15、9/16 的逐日 VaR 预测做样本外回测（次日实际收益 < −VaR 记为违规），
     输出违规日期序列 + 失败率（期望 ≈ 5%/1%）+ **Kupiec LR_uc 的 p 值 + Christoffersen LR_ind（独立性，必做，与 LR_uc 并列输出）**；
     违规日期时间线图 `figures/backtest_exceptions.png`（标注对应市场事件）。
  2. **「存疑」日稳健性复核（默认规则落地）**：阶段一 16 条无事件支撑/与 3141 反向的异常日是否落入违规——给出「含/不含两版回测」对比，
     任何命中先核 3141 同向性与事件窗口再归因；检验结论是否对个别存疑日敏感（延续「只标注不改数」政策，不改收益序列，仅报告敏感性）。
- **下午**
  3. **分段评估**：按 9/14 定义的平稳/高波动段拆分——两模型 × 各置信度在每段的失败率、**Kupiec + Christoffersen 两项**、平均 VaR、期望超损（ES/shortfall 参照）；
     回答「谁在高波动段更稳、谁在平稳段更省资本、谁的违规会聚集」。
  4. **对比结论初稿**：GARCH 条件化是否消除违规聚集；HS 与参数法在极端日（2025-04-10、2022 年加息、2026 波动日）谁先反应；
     双口径下 VaR 量级差（人民币口径高出主口径的部分是否 ≈ 汇率风险）能否被归因；各自的优势场景、局限与优化方向。

- **当日验收：** `results/backtest_results.csv`（每模型 × 置信度 × 口径的失败率 / LR_uc p / **LR_ind p** / 违规数 / 分段统计）；
  违规判定表 + 对比图落盘；两模型优劣结论有一版可写进报告的草稿。

---

### 9/18（周五）收口：《模型验证分析报告》 + 周报

**当日目标：** 汇总成可直接交导师的阶段二交付物，提交入库，并向第 3 周（压力测试）交出参数与基准。

- **上午**
  1. 撰写 **`docs/phase2_model_validation.md`（模型验证分析报告）**：VaR 口径（复权主口径 + 人民币次口径）→ 两种模型实现 → 回测框架与统计检验
     （失败率 + Kupiec + Christoffersen）→ 结果数据表 → 平稳/高波动分段对比 → 模型对比与适用性结论 → 局限与优化方向
     （含 16「存疑」日敏感性、复权/分红边界、人民币口径 CNY 尾 5 日等）。
  2. 质量复核：`code/var_*.py` 从 `factor_table_nav_tr.csv` 一键重跑可复现（结果一致）；代码注释补全；`README.md` 补目录（`results/`）与阶段二文档链接。
- **下午**
  3. Git 提交全部产物（阶段二交付物）；撰写 **`docs/week2_report.md`（周报要点）**：双口径两模型核心结果、回测三件套检验、推荐口径、
     遗留问题 + 第 3 阶段衔接（基准组合暴露与模型参数即压力测试的输入）。
  4. 预留「阶段二给阶段三的参数交接清单」：组合因子暴露 δ（USD 主口径 + 人民币次口径）、各情景相关的波动/相关性参数、事件时间线索引——写进周报。

- **当日验收：** 模型代码包（参数法/历史模拟/回测）+ 模型验证分析报告 + 周报全部入库（commit/push）；可从零复现。

---

## 三、备选缓冲

- **9/19（周六，机动）**：进度滞后时补回测/调模型参数；超前则预研第 3 阶段压力情景（从 `events/risk_events_timeline.csv` 挑选近 5 年 3 次典型冲击并提取因子最大波动，供历史情景参数预演；双口径暴露 δ 直接作为情景映射输入）。

---

## 四、风险与应对

1. **GARCH(1,1) 拟合异常（不收敛 / α+β≈1 / 残差厚尾）** → 备选：Student-t 残差、IGARCH、或 EWMA(λ=0.94) 条件波动率对照（`arch` 均支持）；以对数似然与违规检验选优，并如实报告参数敏感性。
2. **δ-normal 协方差窗口受限**：FRED OAS 仅 2023-09 起 → 因子协方差用内部因子（Δ5Y/Δ10Y/利差代理，2021-08 起）做主；外部 OAS 仅作校验，不进入 VaR 协方差（9/15 写明口径）。
3. **250 窗 1% 分位观测不足**（仅 ~2.5 个有效观测）→ 样本外 ~900+ 日（2022-08 起）下 99% 期望违规 ~9 次、Kupiec/Christoffersen 有基本检验力；并叠加窗宽稳健性与 Filtered-HS 佐证；在报告里说明预研级样本的检验力边界（独立性检验同样受限于样本，结论以点估计 + 区间 + 分段一致性为准）。
4. **分红复权边界**：复权依赖基金派息记录，若个别除息日无官方记录或金额口径（毛/净）存疑 → 以记档为准、不臆造；复核复权前后 σ 变化，
   确认「除息跳空高估波动」已消除（9/14 已据导师反馈完成）；残留风险只存在于记录边界外，不作为主结论依据。
5. **人民币次口径的汇率尾 5 日缺口**：CNY/USD FRED 滞后 → 次口径该段记 NaN，不得用主口径 VaR 替代或静默补齐；
   美元主口径不受影响（δ_fx=0）。FRED 补发后一键重跑自动补全真值。
6. **个别「存疑」异常日主导结论** → 默认规则：一律不修数（延续阶段一政策），「含/不含」两版回测为必做，先核 3141 同向性与事件再归因，
   让结论对单日不脆弱。
